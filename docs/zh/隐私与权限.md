# 隐私与权限

## 数据策略

- PasteMind 不上传任何剪贴板内容。
- 文字与图片历史仅保存在本机应用数据目录。
- 图片以文件方式存储，数据库仅保留索引和元数据。

## 默认隐私保护

- 默认排除常见密码管理器来源（bundle id 黑名单）。
- 图片默认大小上限 10MB，超限不记录。
- 历史默认保留最近 500 条。

## 权限说明

- 自动粘贴依赖 macOS 辅助功能权限。
- 未授权时仍可复制回剪贴板，但不会执行自动粘贴。
